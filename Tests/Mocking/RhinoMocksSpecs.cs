using NUnit.Framework;

namespace Tests.Mocking
{
    [TestFixture]
    public class RhinoMocksSpecs
    {
        [SetUp]
        public void SetUp()
        {
            //create mock for repository

            //create consumer with the mock
        }

        //test that the save method is called when consumer saves the actor

        //setup mock to return actor when adding them to the movie
    }
}